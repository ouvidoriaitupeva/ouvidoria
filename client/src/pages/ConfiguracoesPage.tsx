/* Design: configurações da Ouvidoria de Itupeva — fonte de dados e preferências reunidas em uma área administrativa clara. */
import { useState } from "react";
import * as XLSX from "xlsx";
import { Check, FileSpreadsheet, Info, ShieldCheck, Upload } from "lucide-react";
import OuvidoriaLayout from "@/components/OuvidoriaLayout";
import { metricsFromSheet } from "./Home";

export default function ConfiguracoesPage() {
  const [fileName, setFileName] = useState("metricas.json");
  const [period, setPeriod] = useState("Últimos 12 meses");
  return <OuvidoriaLayout><div className="mx-auto max-w-4xl"><div className="mb-8"><div className="mb-2 flex items-center gap-2 text-xs font-bold uppercase tracking-[0.18em] text-[#D78B1D]"><span className="h-px w-6 bg-[#D78B1D]" />Preferências</div><h1 className="font-display text-4xl font-semibold tracking-tight text-[#173B5E] md:text-5xl">Configurações</h1><p className="mt-2 max-w-2xl text-[16px] leading-7 text-[#5E707A]">Escolha a fonte temporária dos indicadores e o padrão de visualização deste navegador.</p></div><section className="rounded-2xl border border-[#E4DED2] bg-white/80 p-6 sm:p-8"><div className="flex items-start gap-3"><FileSpreadsheet className="mt-1 text-[#00549D]" size={23} /><div><h2 className="font-display text-2xl font-semibold">Fonte dos dados</h2><p className="mt-1 text-sm leading-6 text-[#71818B]">Enquanto a API não estiver conectada, a planilha pode ser carregada localmente para calcular os indicadores.</p></div></div><div className="mt-6 rounded-xl border border-[#D7E7E6] bg-[#EAF4F2] p-4"><div className="flex items-start gap-3 text-sm leading-6 text-[#2E7563]"><ShieldCheck size={18} className="mt-1 shrink-0" /><p><strong>Privacidade:</strong> o arquivo é lido somente neste navegador. Ele não é enviado ao servidor nem publicado no site.</p></div></div><div className="mt-6 flex flex-col gap-4 rounded-xl border border-[#E4DED2] bg-[#FAF9F5] p-5 sm:flex-row sm:items-center sm:justify-between"><div><div className="text-xs font-bold uppercase tracking-widest text-[#71818B]">Fonte atual</div><div className="mt-1 font-bold text-[#173B5E]">{fileName}</div></div><label className="inline-flex cursor-pointer items-center justify-center gap-2 rounded-lg bg-[#173B5E] px-4 py-3 text-sm font-extrabold text-white hover:bg-[#0F2D48]"><Upload size={16} /> Carregar XLSX<input type="file" accept=".xlsx,.xls" onChange={async (event) => { const file = event.target.files?.[0]; if (!file) return; try { const workbook = XLSX.read(await file.arrayBuffer(), { type: "array", cellDates: true, dense: true }); const sheet = workbook.Sheets[workbook.SheetNames[0]]; const rows = XLSX.utils.sheet_to_json<Record<string, unknown>>(sheet, { defval: null, raw: false }); const metrics = metricsFromSheet(rows); localStorage.setItem("falabr-metrics", JSON.stringify(metrics)); setFileName(file.name); window.dispatchEvent(new Event("falabr-metrics-updated")); } catch { setFileName("Erro ao ler a planilha"); } event.target.value = ""; }} className="sr-only" /></label></div></section><section className="mt-6 rounded-2xl border border-[#E4DED2] bg-white/80 p-6 sm:p-8"><div className="flex items-start gap-3"><Check className="mt-1 text-[#0A8F4D]" size={22} /><div><h2 className="font-display text-2xl font-semibold">Padrões da visão geral</h2><p className="mt-1 text-sm leading-6 text-[#71818B]">Essas preferências ficam apenas neste navegador.</p></div></div><label className="mt-6 block text-sm font-bold">Período inicial<select value={period} onChange={(event) => setPeriod(event.target.value)} className="mt-2 h-12 w-full rounded-lg border border-[#DCE3E4] bg-white px-3 text-sm"><option>Últimos 12 meses</option><option>Últimos 6 meses</option><option>Este mês</option><option>Período personalizado</option></select></label><div className="mt-5 rounded-xl bg-[#F1F4F4] p-4 text-sm leading-6 text-[#5E707A]"><Info className="mr-2 inline text-[#00549D]" size={16} />Os filtros de Concluídos, Abertos e Registrados controlam diretamente os dados exibidos nas listas e na evolução.</div></section></div></OuvidoriaLayout>;
}
